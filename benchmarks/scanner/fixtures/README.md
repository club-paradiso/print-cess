# Scanner benchmark fixtures

`ci-smoke.json` is deterministic synthetic data used only to verify the benchmark scorer and CI gate behavior. It is not a real-device performance result.

Real-device observation files should be created from `../real-device-template.json` and should contain only public-safe or purpose-made synthetic document content.
